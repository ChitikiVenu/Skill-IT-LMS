# Vercel deployment trigger

This file intentionally triggers a fresh deployment from the current `main` branch.

Stage 2 database and authentication foundation deployment trigger: 2026-08-25.

The production build must use the latest `package.json` dependency versions.
